# CLAUDE.md

This repository's contributor contract lives in **[`AGENTS.md`](AGENTS.md)**.
Read it before making any change.

Short version: tests first, `npm run verify` before every commit, update
`CHANGELOG.md` in the same commit, never commit secrets, and keep the socket and
queue contract in sync with the `orkestro` master repository.
